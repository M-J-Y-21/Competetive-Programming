#include <bits/stdc++.h>

using namespace std;

int main() {
    /**
     * @brief Super speed boost
     */
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int m, n, area;

    cin >> m >> n;
    area = m * n;
    cout << area / 2 << endl;

    return 0;
}